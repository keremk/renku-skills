# Artifact Pinning And Regeneration

Use targeted iteration:

- Pin good artifacts before broad prompt experiments.
- Regenerate the smallest useful scope.
- When a prompt changes, understand which downstream artifacts become dirty.
- When a model changes, revise prompts if the new model uses a different workflow style.
- If the user dislikes only one segment, avoid rewriting the entire video unless continuity requires it.
